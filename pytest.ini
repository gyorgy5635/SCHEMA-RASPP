[pytest]
testpaths = tests
markers =
    slow: end-to-end RASPP runs (tens of seconds)
# Run with the `pytest` console script (not `python -m pytest`): the latter puts
# the repo root on sys.path, where the local pdb.py shadows the stdlib debugger
# that pytest imports. See PORTING.md ("Known follow-ups").
